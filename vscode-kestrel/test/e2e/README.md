# E2E Test Placeholder

Extension-host end-to-end tests are intentionally deferred.

Planned coverage:
- CodeLens rendering in the editor for `test("name", fn)` calls
- Command execution wiring for `kestrel.runTest` and `kestrel.debugTest`
- Smoke validation of language server startup and request routing in the Extension Development Host
