# Implementation plan (retired)

The long-form phased plan that used to live in this file is **retired** (2026-03-29). It duplicated material in `docs/specs/` and drifted from the codebase; story **39** (`docs/kanban/done/39-implementation-plan-audit.md`) already recorded that the phase checkboxes were going stale.

**Use these instead:**

| Need | Where |
|------|--------|
| Language, bytecode, stdlib, runtime contracts | `docs/specs/` |
| Roadmap, sequencing, delivery | `docs/kanban/README.md` and stories under `docs/kanban/` |
| Build, test commands, agent entry | `AGENTS.md` (repo root) |
| User-oriented overview | `docs/guide.md` |

Older kanban entries may still mention this filename or historical “Phase N” wording; that is archival context only.
