# VM Unit and Integration Tests

## Sequence: 05
## Tier: 2 — Harden the runtime
## Former ID: 100

## Summary

The VM has essentially no meaningful tests. There are only a few trivial Zig tests (placeholder, value size, minimal .kbc load). No tests exist for systematic opcode execution, GC correctness, closure handling, record operations, ADT matching, exception handling, or multi-module loading. This makes it difficult to refactor or fix VM bugs with confidence.

## Current State

- `main.zig`: trivial placeholder test.
- `value.zig`: `value size` test.
- `load.zig`: loads a minimal `.kbc` fixture; path and fixture presence should be verified when adding tests.
- Dedicated `vm/test/unit/` and `vm/test/integration/` may be empty (`.gitkeep` only) depending on layout.
- Fixtures: minimal `empty.kbc` or equivalent may exist; expand with hand-crafted or compiler-generated bytecode.

## Acceptance Criteria

- [ ] **Value tests**: Tag encoding/decoding for int, bool, unit, char, ptr, fn_ref. Boundary values (max/min 61-bit int).
- [ ] **Arithmetic tests**: ADD, SUB, MUL, DIV, MOD, POW with various int values. Edge cases (zero, negative, large).
- [ ] **Comparison tests**: EQ, NE, LT, LE, GT, GE for ints and bools.
- [ ] **Control flow tests**: JUMP, JUMP_IF_FALSE with hand-crafted code buffers.
- [ ] **Record tests**: ALLOC_RECORD, GET_FIELD, SET_FIELD, SPREAD.
- [ ] **ADT tests**: CONSTRUCT, MATCH dispatch by constructor tag.
- [ ] **Closure tests**: LOAD_FN, MAKE_CLOSURE, CALL_INDIRECT for both fn_ref and closure.
- [ ] **Exception tests**: TRY, THROW, END_TRY -- handler invocation, normal completion.
- [ ] **GC tests**: Allocate many objects, trigger GC, verify no use-after-free. Mark/sweep correctness.
- [ ] **Loader tests**: Additional .kbc fixtures (multi-function, with ADT table, with shape table).
- [ ] Fixtures can be hand-crafted byte arrays or generated by the compiler.

## Related

- Sequence **25** (VM test fixtures) is a smaller, fixture-focused slice; prefer folding fixture work into this story unless splitting helps incremental delivery.

## Spec References

- 08-tests §2.4 (Bytecode and VM: each instruction covered by at least one test)
- 08-tests §2.5 (Runtime model: tagged values, heap objects, GC)
