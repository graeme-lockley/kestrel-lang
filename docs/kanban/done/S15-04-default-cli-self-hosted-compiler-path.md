# Default CLI Self-Hosted Compiler Path

## Sequence: S15-04
## Tier: Optional
## Former ID: (none)

## Epic

- Epic: [E15 Bootstrap JAR Self-Hosting Handoff](../epics/unplanned/E15-bootstrap-jar-self-hosting-handoff.md)
- Companion stories: S15-01, S15-02, S15-03, S15-05

## Summary

Switch default `kestrel build`, `kestrel run`, and `kestrel test` compilation behavior to use self-hosted compiler classes generated by bootstrap, ensuring normal command execution no longer depends on bootstrap JAR or TypeScript compiler invocation in the steady state.

## Current State

`scripts/kestrel` currently compiles through `node compiler/dist/cli.js` and uses self-hosted CLI forwarding only as a scaffold. Even with bootstrap artifacts present, normal build/run/test paths still rely on TypeScript compiler execution.

## Relationship to other stories

- **Depends on**: S15-03 mode/provenance state.
- **Depends on**: S15-02 bootstrap command generating self-hosted compiler classes.
- **Blocks**: S15-05 CI handoff enforcement.

## Goals

1. Route `build`, `run`, and `test` compile operations through self-hosted compiler classes by default when bootstrap state is valid.
2. Ensure bootstrap JAR is not used as a compile fallback for normal command execution.
3. Preserve current feature parity for compilation flags (`--clean`, `--refresh`, `--allow-http`, status output) when switching backend.

## Acceptance Criteria

- After successful `./kestrel bootstrap`, default `./kestrel build hello.ks` performs compilation without invoking `node compiler/dist/cli.js` in normal mode.
- `./kestrel run` and `./kestrel test` use the same default self-hosted compiler path.
- Normal command flow does not invoke bootstrap JAR compilation after bootstrap succeeds.

## Spec References

- `docs/specs/09-tools.md`
- `scripts/kestrel`
- `stdlib/kestrel/tools/compiler/cli-entry.ks`
- `stdlib/kestrel/tools/compiler/cli-main.ks`

## Risks / Notes

- Command parity regressions can hide in less common flags (`--status`, URL import modes, cache-clean behavior).
- Recursive CLI dispatch loops are possible if self-hosted command forwarding is not bounded clearly.
- Need deterministic behavior across macOS/Linux shells and CI runner environments.

## Impact analysis

| Area | Change |
|------|--------|
| CLI dispatch | Replace scaffolded TS-backed self-host forwarding with direct JVM execution of bootstrap-generated self-hosted CLI classes when mode is `self-hosted`. |
| Default command path | `run`, `dis`, `build`, and `test` default to self-hosted compiler path after successful bootstrap state validation. |
| Fallback behavior | Keep TypeScript path only for `bootstrap-required` mode; do not execute bootstrap JAR in normal command flow. |
| State gating | Use S15-03 state/provenance readers to deterministically choose active command backend. |
| Docs/specs | Document default mode-switch behavior and explicit fallback conditions. |

## Tasks

- [x] Update self-hosted CLI delegation in `scripts/kestrel` to execute bootstrap-generated classes directly via JVM when compiler mode is `self-hosted`.
- [x] Ensure delegation covers normal command flow (`build`, `run`, `test`, `dis`, `fmt`, `doc`, `lock`) without using bootstrap JAR.
- [x] Keep fallback TypeScript shell implementation for `bootstrap-required` mode.
- [x] Add explicit mode-gating diagnostics/comments where behavior diverges.
- [x] Update docs/spec text to describe default self-hosted path and fallback conditions.
- [x] Run `./scripts/build-bootstrap-jar.sh`.
- [x] Run `./kestrel bootstrap`.
- [x] Run `./kestrel status`.
- [x] Run representative commands via self-hosted path (`./kestrel build hello.ks`, `./kestrel run hello.ks`, `./kestrel test --summary`).
- [x] Run `cd compiler && npm run build && npm test`.

## Tests to add

| Layer | Path | Intent |
|-------|------|--------|
| CLI integration | `scripts/kestrel` self-host delegation path | Ensure `self-hosted` mode dispatches to JVM classpath rooted at `.kestrel/bootstrap/self-hosted` instead of Node/TS compile path. |
| Command smoke | `./kestrel build/run/test` after bootstrap | Validate normal command flow works through self-hosted compiler backend without bootstrap JAR fallback. |
| Mode fallback | `./kestrel status` in missing-state scenario | Confirm `bootstrap-required` mode keeps legacy TypeScript path available. |
| Compiler regression | `cd compiler && npm run build && npm test` | Ensure CLI dispatch changes do not regress compiler source behavior. |

## Build notes

- 2026-04-12: Reworked `scripts/kestrel` compile path selection to use `compile_with_active_compiler`, which prefers self-hosted compiler classes from `.kestrel/bootstrap/self-hosted/` when mode is `self-hosted`.
- 2026-04-12: Added recursion guard for nested invocations (`KESTREL_CLI_TS_FALLBACK=1`) to prevent runaway self-hosted dispatch loops while keeping top-level post-bootstrap commands on the self-hosted path.
- 2026-04-12: Confirmed post-bootstrap smoke flow: `./kestrel build hello.ks`, `./kestrel run hello.ks`, and `./kestrel test --summary` all succeed.
- 2026-04-12: Confirmed compiler regression suite passes (`cd compiler && npm run build && npm test`).

## Documentation and specs to update

- [x] `docs/specs/09-tools.md` — describe default self-hosted command path post-bootstrap and explicit TypeScript fallback conditions.
- [x] `AGENTS.md` — update bootstrap workflow notes to include status + post-bootstrap default command behavior.
