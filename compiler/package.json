{
  "name": "kestrel-compiler",
  "version": "0.1.0",
  "description": "Kestrel compiler (TypeScript) — parse, typecheck, emit .kbc",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "~5.6.0",
    "vitest": "~2.1.0"
  }
}
